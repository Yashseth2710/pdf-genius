# PDF Genius - backend runtime dependencies (Python 3.12)
# Exact versions are pinned in requirements.lock.txt

# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart

# --- Config & validation ---
pydantic
pydantic-settings
email-validator

# --- Database ---
SQLAlchemy
alembic
psycopg[binary]

# --- Auth & security ---
PyJWT
argon2-cffi

# --- PDF & image processing ---
PyMuPDF
pypdf
pdfplumber
pillow
# Reads HEIC, which is what an iPhone writes by default.
pillow-heif

# --- Rate limiting ---
# In-memory by default. `redis` is only imported when REDIS_URL is set, which
# is how a serverless deployment gets limits that hold across instances.
slowapi
redis

# --- HTTP client (AI providers, health checks) ---
httpx
